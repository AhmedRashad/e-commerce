import { Link } from "react-router-dom";

const NavBar = (props) => {
  return (
    <nav className="bg-indigo-600 w-full py-2 px-4 text-white flex flex-row-reverse">
      
    </nav>
  );
};

export default NavBar;
